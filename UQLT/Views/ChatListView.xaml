<UserControl x:Class="UQLT.Views.ChatListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:vm="clr-namespace:UQLT.ViewModels"
             xmlns:chat="clr-namespace:UQLT.Models.Chat"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="782" d:DesignWidth="280">

    <Grid Margin="10">
        <TreeView x:Name="FriendList" HorizontalAlignment="Left" Height="782" VerticalAlignment="Top" Width="280" ItemsSource="{Binding RosterGroups}">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type chat:RosterGroup}" ItemsSource="{Binding Members}">
                    <StackPanel Orientation="Horizontal">
                        <!--Image Source="/WpfTutorialSamples;component/Images/group.png" Margin="0,0,5,0" />-->
                        <TextBlock Text="{Binding Name}" />
                        <TextBlock Text=" [" Foreground="Green" />
                        <TextBlock Text="{Binding Members.Count}" Foreground="Green" />
                        <TextBlock Text="]" Foreground="Green" />
                    </StackPanel>
                </HierarchicalDataTemplate>
                <DataTemplate DataType="{x:Type chat:RosterMember}">
                    <StackPanel Orientation="Horizontal">
                        <!--<Image Source="/WpfTutorialSamples;component/Images/user.png" Margin="0,0,5,0" />-->
                        <TextBlock Text="{Binding Name}" />

                        <!--This will ecome the status-->
                        <!--<TextBlock Text=" (" Foreground="Green" />-->
                        <!--<TextBlock Text="{Binding Age}" Foreground="Green" />-->
                        <!--<TextBlock Text=" years)" Foreground="Green" />-->
                    </StackPanel>
                </DataTemplate>
            </TreeView.Resources>
        </TreeView>
    </Grid>
</UserControl>
